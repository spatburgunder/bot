{
  "builds": [
    {
      "src": "./dockerfile",
      "use": "@vercel/docker",
      "config": {
        "runtime": "experimental-edge"
      }
    }
  ],
  "routes": [
    { "src": "/.*", "dest": "/" }
  ]
}
