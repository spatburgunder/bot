{
  "builds": [
    {
      "src": "./Dockerfile",
      "use": "docker",
      "config": {
        "runtime": "experimental-edge"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/" }
  ]
}
