{
  "builds": [
    {
      "src": "./dockerfile",
      "use": "docker",
      "config": {
        "runtime": "experimental-edge"
      }
    }
  ],
  "routes": [
    { "src": "/.*", "dest": "/" }
  ]
}
